name: ARC Open
on:
  workflow_dispatch:

jobs:
  Explore-GitHub-Actions:
    runs-on: arc-runner-set-open
    steps:
    - run: |
        echo "🎉 This job uses runner scale set runners!"
        ps aux
        for ((i=1; i<=100; i++)); do
          echo "this is the open runner, what can it find?"
          sleep 10
        done
